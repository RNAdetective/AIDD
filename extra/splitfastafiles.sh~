#!/usr/bin/env bash

while read line
do
    if [[ ${line:0:1} == '>' ]]
    then
        outfile=${line:1:8}.fa
        echo $line > sequenceofinterest$outfile
    else
        echo $line >> sequenceofinterest$outfile
    fi
done < sequenceofinterestcleaner.fa

